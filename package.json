{
  "name": "api-rss-nextjs",
  "version": "1.0.0",
  "description": "A RESTful API service that retrieves, geocodes, and provides data from the Swedish Police public RSS feed of events",
  "main": "api/simple-server.js",
  "private": true,
  "scripts": {
    "start": "cd api && PORT=80 HOSTNAME=0.0.0.0 NODE_ENV=production node simple-server.js",
    "build": "cd api && npm run build",
    "install": "cd api && npm install",
    "dev": "cd api && npm run dev:custom"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 